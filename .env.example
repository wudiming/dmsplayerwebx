# ============================================================
# SPlayer-Web 环境变量配置说明 (示例文件)
# ============================================================
#
# 【网易云音乐增强版音源服务 (自建 API 服务器)】
#
# 简介：
# 本项目支持接入自建的 NeteaseCloudMusicApi Enhanced 服务，
# 提供更丰富的无损音源获取与歌曲解灰能力。
# 开源项目地址：https://github.com/NeteaseCloudMusicApiEnhanced/api-enhanced
#
# api-enhanced 项目官方 Docker 极简部署命令：
#   docker run -d -p 3000:3000 --name ncm-api moefurina/ncm-api:latest
#
# 环境变量规则：
# - 若配置此项（URL 非空）：系统将自动激活“网易云·增强版”音源插件，并以该服务作为音源与解灰接口。
# - 若未配置此项（留空或未定义）：该插件保持关闭状态（enabled: false），仅使用官方标准接口。
#
# SPlayer-Web Docker 运行示例：
#   docker run -d -p 5173:5173 \
#     -e NETEASE_ENHANCED_URL=http://<your-server-ip>:3000 \
#     splayer-web:latest
#
# 本地运行示例 (复制本文件为 .env 并填入您自建的服务地址)：
NETEASE_ENHANCED_URL=http://localhost:3000
